## Available Scripts

In the project directory, you can run:

### `npm start`

## folfer structure

/Component
    /SubComponents
        //Will have the same structre as its parent
    index.tsx
    queries.ts (if needed)
    styled.ts
